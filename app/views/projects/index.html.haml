- @page_title = 'Team Projects'
%a.btn.btn-primary{href: new_project_path} New Project
%table.tablelist
  %thead
    %tr
      %th Name
      %th
      %th
      %th
  %tbody
    - @projects.each do |project|
      %tr
        %td= link_to project.name, project_issues_path(project)
        %td= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' }
%br/
